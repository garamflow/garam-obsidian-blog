# 상품 React-Router 설정
## 1) react-router-dom 를 이용한 라우팅 설정
- react-router-dom을 App.js에 연결해주고, `root.js` 파일을 생성해서 라우터 설정을 해준다.
- 라우터를 설정할 때는 코드 스플리팅을 적용해준다.
```javascript
// src/App.js

import { RouterProvider } from 'react-router-dom';
import root from './router/root';

function App() {
	return (
		<RouterProvider router={root} />
	);
}

export default App;
```

```javascript
// src/router/root.js

import { Suspense, lazy } from 'react';
import productsRouter from './productsRouter';  

const { createBrowserRouter } = require('react-router-dom');

const Loading = <div>Loading...</div>;

const MainPage = lazy(() => import('../pages/MainPage'));

const root = createBrowserRouter([
	{
		path: "",
		element: <Suspense fallback={Loading}><MainPage /></Suspense>,
	}
])

export default root;
```

```javascript
import React from 'react';
import BasicLayout from '../layouts/BasicLayout';

const MainPage = () => {
	return (
		<div>
			<div className='text-4xl'>Main Page</div>
		</div>
	);
}

export default MainPage;
```

## 2) 공통 레이아웃 적용하기
```javascript
// src/layouts/BasicLayout.js

import React from 'react';
import BasicMenu from '../components/menus/BasicMenu';

const BasicLayout = ({ children }) => {
	return (
	<>
		<BasicMenu />
		<div className='bg-white w-full flex flex-col space-y-4 md:flex-row md:space-x-4
		md:space-y-0'>
			<main className='bg-blue-400 md:w-2/3 lg:w-3/4 px-5 py-5'>
				{children}
			</main>
			<aside className='bg-green-500 md:w-1/3 lg:w-1/4 px-5 py-5 flex'>
				<h1 className='text-2xl md:text-4xl'>sidebar</h1>
			</aside>
		</div>
	</>
	)
}

export default BasicLayout;
```

```javascript
// src/components/menus/BasicMenu.js

import React from 'react'
import { Link } from 'react-router-dom'

const BasicMenu = () => {
	return (
		<nav id='navbar' className=" flex bg-blue-300">
			<div className="w-4/5 bg-gray-500" >
			<ul className="flex p-4 text-white font-bold">
				<li className="pr-6 text-2xl">
					<Link to={'/'}>Main</Link>
				</li>
				<li className="pr-6 text-2xl">
					<Link to={'/about'}>About</Link>
				</li>
				<li className="pr-6 text-2xl">
					<Link to={'/products/'}>Product</Link>
				</li>
			</ul>
			</div>
			<div className="w-1/5 flex justify-end bg-orange-300 p-4 font-medium">
				<div className="text-white text-sm m-1 rounded" >Login</div>
			</div>
		</nav>
	)
}

export default BasicMenu
```


